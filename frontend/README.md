### 프로젝트 실행

**1. 프로젝트 루트(`frontend`)로 이동** 
```
cd frontend
```

**2. 패키지 설치** 
```
npm install
```

**3. 실행**
```
npm run dev
```

### 주요 폴더 구조

**전체 구조**
```bash
📁 frontend                     
 ├── 📁 public                  // 정적 파일
 ├── 📁 src                     // 소스 코드
 │    ├── 📁 assets             // 이미지, 폰트 등 정적 리소스
 │    ├── 📁 component          // 재사용 UI 컴포넌트
 │    ├── 📁 container          // 페이지/큰 구조의 컴포넌트
 │    ├── 📁 hooks              // 커스텀 React Hooks
 │    ├── 📁 utils              // 유틸 함수 모음
 │    ├── 📄 App.css            // App 전용 스타일
 │    ├── 📄 App.tsx            // 최상위 컴포넌트(Root Component)
 │    ├── 📄 global.d.ts        // 타입 보완용 전역 선언 파일
 │    └── 📄 index.css          // 글로벌 스타일 & Tailwind 엔트리
 ├── 📄 .env                    // 개발환경용 환경변수
 ├── 📄 .env.production         // 프로덕션 환경변수
 ├── 📄 .gitignore              // Git 제외 파일 설정
 ├── 📄 index.html              // SPA 기본 HTML 템플릿
 ├── 📄 tailwind.config.js      // Tailwind 설정 파일
 ├── 📄 tsconfig.app.json       // App 전용 TypeScript 설정
 ├── 📄 tsconfig.json           // 전체 TS 설정 메인 파일
 ├── 📄 tsconfig.node.json      // Node(Vite)용 TS 설정
 └── 📄 vite.config.ts          // Vite 빌드/개발 설정
 ```

### 기본 설정

**파비콘 변경** 
`frontend/public/image.png` 변경 

**로고 변경(어드민 로그인 및 관리자 프로필)**
`frontend/src/assets/profile.png` 변경 

**테마 색상 변경**
`tailwind.config.js`에서 colors 변경

**어드민 메뉴 변경**


